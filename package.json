{
  "name": "media-bank",
  "version": "1.0.0",
  "description": "Full-stack media bank with uploads, thumbnails, metadata, and search",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "build": "node scripts/package.js",
    "start:one": "sh -c 'test -d node_modules || npm ci --omit=dev; PORT=${PORT:-3000} NODE_ENV=${NODE_ENV:-production} node server.js'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mime-types": "^2.1.35",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.4",
    "better-sqlite3": "^9.4.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
